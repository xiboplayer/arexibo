name: Build Arexibo RPM

on:
  push:
    tags: ['v*']
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

permissions:
  contents: write

jobs:
  build:
    uses: xiboplayer/.github/.github/workflows/build-rpm.yml@9af382cf12830bca426a1a99e4b59532ee72a711  # main
    with:
      package-name: arexibo
      rpm-spec: 'rpm/arexibo.spec'
      fedora-matrix: '["43", "44"]'
      arch-matrix: '["x86_64", "aarch64"]'
      source-tarball: 'full'
      gpg-sign: true
      default-version: '0.4.1'
      publish-to-repo: true
    secrets: inherit
